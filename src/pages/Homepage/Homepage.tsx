import React from "react";
import './Homepage.css'
import Carousel from "../../components/Carousel/Carousel";

const Homepage = () => {
   return (
      <section id="homepage">
         <Carousel />
      </section>
   );
};

export default Homepage;
